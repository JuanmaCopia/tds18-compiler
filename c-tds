if [ $# -eq 0 ]
  then
    echo "No source code supplied"
  else
    ./build/parser.out $1
    if [ -e assembly.s ] 
      then
        gcc -g -o build/executable assembly.s src/externs.c
        mv assembly.s ./build
        ./build/executable
      else
        echo " "
    fi
fi